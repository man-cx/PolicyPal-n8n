// Export all common components for easier imports

// Card components
export { default as PolicyCard, SimpleCard } from './common/Card';

// Loading components
export { 
  default as LoadingIndicator, 
  LoadingScreen, 
  LoadingOverlay 
} from './common/Loading';

// Modal components
export { 
  default as Modal, 
  ConfirmationModal 
} from './common/Modal';

// Header components
export { 
  default as Header,
  SectionHeader
} from './common/Header';

// Button component
export { default as Button } from './common/Button'; 